// Accessing objects

const hero = {
  name: "Arc Warden",
  type: "Core",
  lane: "Mid",
};

// 1 Dot method
console.log(hero.name);
console.log(hero.type);
console.log(hero.lane);

// 2 Bracket notation
console.log(hero["name"]);
console.log(hero["type"]);
console.log(hero["lane"]);

// Ternary operator - short form of the if else statement
// condition ? true : false

const marks = 50;
const result = marks > 35 ? "Pass" : "Fail";
console.log(result);

// eg2
const user = "admin";
const login = user === "admin" ? "Login Successful" : "Login failed";
console.log(login);
